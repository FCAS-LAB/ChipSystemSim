# Diagnostic layer for the quiet native CUDA runtime.
FROM legosim-real:8166851-simbricks-pthread-bfs-cudaquiet

USER root
RUN apt-get update \
    && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends gdb \
    && rm -rf /var/lib/apt/lists/*
