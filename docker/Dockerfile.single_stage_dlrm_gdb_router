# Add only router request auditing to the already built gdb DLRM image.
ARG BASE_IMAGE=chipsystemsim:single-stage-dlrm_cpp_large-gdb
FROM ${BASE_IMAGE}

COPY ChipSystemSim/real/simbricks_pipe_router.py /opt/chipsystemsim-distributed/
